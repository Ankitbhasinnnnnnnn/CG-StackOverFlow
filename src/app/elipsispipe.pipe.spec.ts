import { ElipsispipePipe } from './elipsispipe.pipe';

describe('ElipsispipePipe', () => {
  it('create an instance', () => {
    const pipe = new ElipsispipePipe();
    expect(pipe).toBeTruthy();
  });
});
